---
# defaults file for setup_users

suu_groups_create: true
suu_groups_remove: true
suu_users_addorrm: true

suu_groups:
  - suu_groups_name:
    suu_groups_gid:
    suu_groups_state:

users:
  - setup_users_name:
    setup_users_comment:
    setup_users_state:
    setup_users_group:
    setup_users_shell:
